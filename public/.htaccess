# Tomodachi POS - Control de caché para assets
# Evita que Cloudflare/navegadores sirvan versiones viejas de JS/CSS
<IfModule mod_headers.c>
    <FilesMatch "\.(js|css)$">
        Header set Cache-Control "no-cache, must-revalidate"
    </FilesMatch>
</IfModule>
